import React, { useState, useEffect } from 'react';
import { Typography, Container, Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Button } from '@mui/material';
import axios from 'axios';

const DiscoverPage =({})=>{
    /*TODO:
     * Implement ui for services, adverts etc
     *User friendly ui on discover content, wel structure to dapt to different device screens
     * dark mode support with elegant appearnce on both themes
     *... other discovery services
     * */
    return(<></>)
}

export default DiscoverPage;
