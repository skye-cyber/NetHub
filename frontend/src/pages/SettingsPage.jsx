import React, { useState, useEffect } from 'react';
import { Typography, Container, Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Button } from '@mui/material';
import axios from 'axios';

const SettingsPage =({})=>{
    /*TODO:
     * Implement settings page for admins
     * 1. net configurations
     * add free internet toggles && and payed mode toggle
     * ...etc related settings
     * */
    return(<></>)
}

export default SettingsPage;
